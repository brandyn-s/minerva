# Batch 08: Evaluation and archival preparation

Base: `6f148242c93f2cd44868c6374178ef12a8876586`; branch `docs/scorebook-transition-batch-00`; worktree `/Users/brandyn.schult/code/minerva-scorebook-transition`. Changes remain uncommitted. No deployment or paid model calls. Scope is verified local software, not creative efficacy or hosted durability. See [implementation](implementation.md) for startup and the shared verification record.

## Implemented

The deterministic harness runs independent, Scorebook-style approximation and diversity policies on three development and three held-out seeds at 24 calls each: 18 runs, 432 synthetic calls. All policies find the four synthetic mechanisms; this validates accounting/integration and does not show Minerva superiority. Source receipt fidelity and differences in retention remain inspectable.

## Evidence and limits

Synthetic local scale tests cover 100/1,000/10,000 candidates; 30-item queries remain bounded and reading previews near 13 KB. Full analysis at 10,000 took about 218 ms in the latest recorded run, not a throughput guarantee. Worker restart and browser reload/intervention/probe/materialization pass. A concrete live protocol is prepared; paid evaluation and independent human review remain pending authorization/access.

## Dependency handoff

09 subsequently recovered accessible exports and archived Scorebook with an explicit disposition for remaining unknown coverage; see batch-09.md.
