# Batch 06: Interventions and observed outcomes

Base: `6f148242c93f2cd44868c6374178ef12a8876586`; branch `docs/scorebook-transition-batch-00`; worktree `/Users/brandyn.schult/code/minerva-scorebook-transition`. Changes remain uncommitted. No deployment or paid model calls. Scope is verified local software, not creative efficacy or hosted durability. See [implementation](implementation.md) for startup and the shared verification record.

## Implemented

A challenge records the source reading/candidate, unchanged run goal/constraints and chosen Develop/Wander intent. Explicit submission adds two calls to allowance. Result assessment records whether a mechanism change, preserved constraints and actionability jointly support a textual escape; failed operations remain inconclusive. Results link an updated reading.

## Evidence and limits

A bounded allocation probe executes an explicitly selected central-queue or independent-reservation model and stores observed capacity preservation/violation. Readings incorporate those simulation observations separately from textual judgments. Selecting the model does not validate its correspondence to the idea. No arbitrary generated code or real-world validation. End-to-end synthetic intervention and probe tests pass.

## Dependency handoff

07 exposes the same evidence in the existing Expedition panel; no second discovery UI.
